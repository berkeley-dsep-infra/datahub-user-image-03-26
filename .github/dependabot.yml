version: 2
updates:
  # Configure updates for Conda environment files (environment.yml)
  - package-ecosystem: "conda"
    directory: "/" # Path to the directory containing environment.yml
    schedule:
      interval: "weekly" # Check for updates weekly (can also be 'daily' or 'monthly')
    open-pull-requests-limit: 0

  # Nested pip dependencies inside environment.yml
  - package-ecosystem: "pip"
    directory: "/"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 0  
